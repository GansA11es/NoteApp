<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">
    <item
        android:id="@+id/navigation_bar_item_active_indicator_view"
        android:icon="@drawable/ic_baseline_menu_30"
        app:actionViewClass="com.google.android.material.navigation.NavigationBarMenuView"
        app:showAsAction="always"
        android:title="@string/navigation_title"
        android:visible="true"/>

    <item
        android:id="@+id/app_bar_search"
        android:icon="@drawable/ic_baseline_search_30"
        android:title="@string/search_title"
        app:actionViewClass="android.widget.SearchView"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_settings"
        android:orderInCategory="100"
        android:title="@string/action_settings"
        app:showAsAction="never"/>

</menu>
